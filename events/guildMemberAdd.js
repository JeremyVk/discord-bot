module.exports = {
    name: "guildMemberAdd",
    description: "Envoie un message quand un utilisateur rejoint le serveur pour la premliere fois",
    execute(member) {
        console.log(member)
    }
}